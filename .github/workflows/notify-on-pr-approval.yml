name: Notify Slack on PR Approval

on:
  pull_request_review:
    types: [submitted]

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Check if the review is an approval
        id: check_approval
        run: |
          if [ "${{ github.event.review.state }}" == "approved" ]; then
            echo "approved=true" >> $GITHUB_ENV
          else
            echo "approved=false" >> $GITHUB_ENV
          fi

      - name: Download Slack Message TS
        uses: actions/download-artifact@v2
        with:
          name: slack-message-ts
          path: .

      - name: Read Slack Message TS
        id: read_ts
        run: |
          SLACK_MESSAGE_TS=$(cat slack_message_ts.txt)
          echo "SLACK_MESSAGE_TS=$SLACK_MESSAGE_TS" >> $GITHUB_ENV

      - name: Update Slack Message on Approval
        if: env.approved == 'true'
        uses: slackapi/slack-github-action@v1.24.0
        with:
          payload: |
            {
              "channel": "#your-channel",
              "ts": "${{ env.SLACK_MESSAGE_TS }}",
              "text": ":tada: PR #${{ github.event.pull_request.number }} - *${{ github.event.pull_request.title }}* has been approved! :tada:",
              "attachments": [
                {
                  "fallback": "View the pull request on GitHub",
                  "actions": [
                    {
                      "type": "button",
                      "text": "View PR",
                      "url": "${{ github.event.pull_request.html_url }}",
                      "style": "primary"
                    }
                  ]
                }
              ]
            }
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
